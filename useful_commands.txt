He creado este txt para que compartamos los comandos más recurrentes

SERVIDOR

uvicorn main:app --reload               # Para iniciar el servidor en local
uvicorn main:app 0.0.0:8000 --reload    # Para iniciar el servidor compartido a través de la red local

GIT


VIRTUAL ENVIRONMENT

                                     # Para activar el entorno virtual
pip install -r .\requirements.txt    # Para instalar las dependencias del proyecto